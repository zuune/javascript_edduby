// JavaScript adalah bahasa pemrograman yang digunakan untuk membuat halaman web interaktif.
  // Biasanya disematkan di dalam tag <script> di dalam dokumen HTML.

  // Menulis kode JavaScript dalam HTML (tag <script>).
  // Kode JavaScript dapat ditempatkan di bagian head atau body dokumen HTML.
  // Contoh:

  console.log("Halo, ini adalah pesan dari JavaScript!");

  // Variabel dan tipe data dasar (string, number, boolean).
  // Variabel digunakan untuk menyimpan data.
  // Tipe data string untuk teks, number untuk angka, boolean untuk nilai true/false.

  let nama = "John";
  let umur = 25;
  let isMahasiswa = true;

  // Menggunakan console.log() untuk mencetak hasil.
  // Fungsi console.log() digunakan untuk mencetak output ke konsol browser.

  console.log("Nama:", nama);
  console.log("Umur:", umur);
  console.log("Mahasiswa?", isMahasiswa);

  // Operasi matematika dasar.
  let angka1 = 10;
  let angka2 = 5;

  let hasilTambah = angka1 + angka2;
  let hasilKurang = angka1 - angka2;
  let hasilKali = angka1 * angka2;
  let hasilBagi = angka1 / angka2;

  console.log("Hasil Penambahan:", hasilTambah);
  console.log("Hasil Pengurangan:", hasilKurang);
  console.log("Hasil Perkalian:", hasilKali);
  console.log("Hasil Pembagian:", hasilBagi);